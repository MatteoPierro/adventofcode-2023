#[cfg(test)]
mod tests {
    use indoc::indoc;

    #[test]
    fn it_does_something() {
        let input = indoc! {"
        something here
        and here"};

        assert_eq!(false, true);
    }
}